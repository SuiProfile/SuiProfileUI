export type TestnetVars = {
  packageId: string;
  profileRegistryId: string;
  statsRegistryId: string;
  publisherId: string;
  displayId: string;
  upgradeCapId: string;
  initialSharedVersion: string;
  clockId: string;
  walrusPublisherUrls: string;
  walrusAggregatorUrls: string;
  dashboardCollectionId: string;
};